# NTNT - No Tabs, No Titlebar

Minimal theme for Yakuake without tabbar and titlebar.
Useful when using a terminal multiplexer like tmux.

## Install

Compress the folder into a tarbal and use Yakuake's configuration dialog 
to install the theme. Setup keyboard shortcut to open configuration dialog
before installing the theme.
